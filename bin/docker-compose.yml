version: '3.8'
services:
  oracle:
    image: gvenzl/oracle-xe:21.3.0
    container_name: surveycraft-oracle
    environment:
      - ORACLE_PASSWORD=Oracle123
    ports:
      - "1521:1521"
    volumes:
      - oracle_data:/opt/oracle/oradata
volumes:
  oracle_data:
